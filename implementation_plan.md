# תמיכה בריבוי סביבות (Multi-Environment)

המטרה של שינוי זה היא להפוך את המערכת ל"רב-סביבתית" (Multi-Tenant). כל סביבה תתפקד כפרויקט או מערכת עצמאית לחלוטין (עם מוצרים, ארנקים, משתמשים ועסקאות משלה), וישותף רק מסד הנתונים הכללי.

## User Review Required
> [!IMPORTANT]
> זהו שינוי ארכיטקטוני עצום שידרוש עדכון של כמעט כל קובץ במערכת. 
> - כל הנתונים הקיימים כרגע במסד הנתונים יצטרכו להיות משויכים ל"סביבת ברירת מחדל" שתיצור המערכת או שיימחקו. **האם יש נתונים שחשוב לשמור או שאפשר למחוק את הכל?**
> - משתמש יכול להיות רק בסביבה אחת, או בכמה סביבות? (הנחתי שלכל משתמש רגיל יש סביבה אחת בלבד, ורק מנהל-על יכול לראות הכל).

## Open Questions
1. כרגע במסך "אין הרשאות", המשתמש פותח מייל למנהל. אם משתמש בוחר סביבה, האם להציג לו רק את מנהלי המערכת של **אותה סביבה**, או לשלוח אימייל ל**מנהל-העל** (Super Admin)?
2. האם "מנהל-על" יוכל להתחבר לסביבות שונות כדי לראות את הנתונים שלהן?

## Proposed Changes

### Database (Prisma)
- **[NEW] מודל Environment:** טבלה חדשה שתשמור את פרטי הסביבה (שם, תאריך יצירה, פעיל/לא פעיל).
- **[MODIFY] מודל User:** הוספת שדה `environmentId` וכן הוספת תפקיד `superadmin` ב-`UserRole`.
- **[MODIFY] כל שאר המודלים (Product, Wallet, Category, Transaction, SystemMessage, SystemSettings):** הוספת שדה חובה `environmentId` וקישור לסביבה.

### Backend APIs
- **[MODIFY] Middleware הרשאות:** בכל בקשה לשרת, נוודא לאיזו סביבה שייך המשתמש (לפי ה-Token שלו) ונסנן את המידע מול מסד הנתונים אוטומטית לפי ה-`environmentId` שלו.
- **[MODIFY] כל הראוטים הקיימים:** נעדכן את השאילתות (findMany, create, update) כך שיפעלו רק על הסביבה של המשתמש.
- **[NEW] ראוט Environments:** הוספת נתיב חדש (`/api/environments`) שישמש את ה-`superadmin` ליצירת סביבות חדשות, ולשיוך משתמשים לסביבות אלו.
- **[MODIFY] Auth Routes:** הוספת API שמחזיר רשימה פומבית של הסביבות הפעילות, כדי שמי שאין לו הרשאה יוכל לבחור לאיזו סביבה הוא מבקש להצטרף.

### Frontend
- **[NEW] פאנל Super Admin:** יצירת אזור ניהול חדש לחלוטין שאליו יכולים להיכנס רק משתמשים מסוג `superadmin`. באזור זה הם יוכלו לראות את כל הסביבות, ליצור סביבה חדשה, ולנהל את השיוך של המשתמשים לכל סביבה.
- **[MODIFY] מסך No Access:** נוסיף תפריט גלילה (Dropdown) בו משתמש ללא הרשאות יוכל לבחור את הסביבה אליה הוא רוצה להצטרף. לאחר הבחירה, יוצגו לו מנהלי המערכת הרלוונטיים לאותה סביבה.
- **[MODIFY] ניהול State (Zustand):** נעדכן את חנויות המידע כך שיתמכו בהתנתקות והתחברות לסביבות בצורה חלקה.

## Verification Plan
1. הרצת `npx prisma db push` ועדכון מסד הנתונים.
2. יצירת סביבה ראשונית ומשתמש "מנהל-על".
3. בדיקת יצירת סביבה שניה, וידוא כי משתמש מסביבה א' לא רואה נתונים של סביבה ב'.
4. וידוא שמסך "אין הרשאות" טוען את רשימת הסביבות, ומציג מנהלים בהתאם לסביבה שנבחרה.
